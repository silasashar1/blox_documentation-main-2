get:
  tags:
    - audiences
  summary: List Export Audiences
  description: |-
    List audiences available to export.
  operationId: list_export_audiences_api_v1_workflows_personalization_audiences__get
  responses:
    '200':
      description: Successful Response
      content:
        application/json:
          schema:
            $ref: '../../schemas/AudienceResponse.yaml'
    '400':
      description: client does not exist
      content:
        application/json:
          schema:
            $ref: '../../schemas/ErrorResponse.yaml'
    '404':
      description: config not found
      content:
        application/json:
          schema:
            $ref: '../../schemas/ErrorResponse.yaml'